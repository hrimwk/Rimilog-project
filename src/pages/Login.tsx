import styled from 'styled-components';

function Login() {
  return (
    <LoginContainer>
      <div className="container">로그인</div>
    </LoginContainer>
  );
}
const LoginContainer = styled.div``;
export default Login;
